<div>
    <h1>Example 1: Basic usage</h1>
</div>
<form class="bs-docs-example form-horizontal">
    {{#if model.isValidated}}
        {{#if model.validationResult.hasError}}
            <div class="alert alert-error">
                <strong>Error!</strong><br>
                <ul>
                    {{#each error in model.validationResult.errors}}
                        <li>{{error}}</li>
                    {{/each}}
                </ul>
            </div>
        {{else}}
            <div class="alert alert-success">
                <strong>User is valid!</strong><br>
            </div>
        {{/if}}
    {{/if}}
    <div class="control-group" {{bind-attr class="model.validationResult.name.hasError:error:"}}>
        <label class="control-label" for="inputName">Name</label>
        <div class="controls">
            {{input id="inputName" value=name}}
            <span class="help-inline">{{model.validationResult.name.error}}</span>
        </div>
    </div>
    <div class="control-group" {{bind-attr class="model.validationResult.age.hasError:error:"}}>
        <label class="control-label" for="inputAge">Age</label>
        <div class="controls">
            {{input id="inputAge" value=age}}
            <span class="help-inline">{{model.validationResult.age.error}}</span>
        </div>
    </div>
    <div class="control-group" {{bind-attr class="model.validationResult.email.hasError:error:"}}>
        <label class="control-label" for="inputEmail">Email</label>
        <div class="controls">
            {{input id="inputEmail" value=email}}
            <span class="help-inline">{{model.validationResult.email.error}}</span>
        </div>
    </div>
    <div class="control-group" {{bind-attr class="model.validationResult.zodiac.hasError:error:"}}>
        <label class="control-label" for="inputZodiac">Zodiac</label>
        <div class="controls">
            {{input id="inputZodiac" value=zodiac}}
            <span class="help-inline">{{model.validationResult.zodiac.error}}</span>
        </div>
    </div>
    <div class="control-group" {{bind-attr class="model.validationResult.password.hasError:error:"}}>
        <label class="control-label" for="inputPassword">Password</label>
        <div class="controls">
            {{input id="inputPassword" value=password}}
            <span class="help-inline">{{model.validationResult.password.error}}</span>
        </div>
    </div>
    <div class="control-group" {{bind-attr class="model.validationResult.password.hasError:error:"}}>
        <label class="control-label" for="inputPassword2">Password 2</label>
        <div class="controls">
            {{view Ember.TextField id="inputPassword2" value=password2}}
        </div>
    </div>
    <div class="control-group">
        <div class="controls">
            <button type="submit" class="btn" {{action "clear"}}>Clear</button>
            <button type="submit" class="btn" {{action "validate"}}>Validate</button>
        </div>
    </div>
</form>