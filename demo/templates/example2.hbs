<div>
    <h1>Example 2: Live validation</h1>
</div>
<form class="bs-docs-example form-horizontal">
    {{#if model.isValidated}}
        {{#if model.validationResult.hasError}}
            <div class="alert alert-error">
                <strong>Error!</strong><br>
                <ul>
                    {{#each error in model.validationResult.errors}}
                        <li>{{error}}</li>
                    {{/each}}
                </ul>
            </div>
        {{else}}
            <div class="alert alert-success">
                <strong>User is valid!</strong><br>
            </div>
        {{/if}}
    {{/if}}
    <div {{bind-attr class=":control-group view.inputName.validationResult.hasError:error:"}}>
        <label class="control-label" for="inputName">Name</label>
        <div class="controls">
            {{validator-text-field viewName="inputName" id="inputName" value=model.name}}
            <span class="help-inline">{{view.inputName.validationResult.error}}</span>
        </div>
    </div>
    <div {{bind-attr class=":control-group view.inputAge.validationResult.hasError:error:"}}>
        <label class="control-label" for="inputAge">Age</label>
        <div class="controls">
            {{validator-text-field viewName="inputAge" id="inputAge" value=model.age}}
            <span class="help-inline">{{view.inputAge.validationResult.error}}</span>
        </div>
    </div>
    <div {{bind-attr class=":control-group view.inputEmail.validationResult.hasError:error:"}}>
        <label class="control-label" for="inputEmail">Email</label>
        <div class="controls">
            {{validator-text-field viewName="inputEmail" id="inputEmail" value=model.email}}
            <span class="help-inline">{{view.inputEmail.validationResult.error}}</span>
        </div>
    </div>
    <div {{bind-attr class=":control-group view.inputZodiac.validationResult.hasError:error:"}}>
        <label class="control-label" for="inputZodiac">Zodiac</label>
        <div class="controls">
            {{validator-text-field viewName="inputZodiac"  id="inputZodiac" value=model.zodiac}}
            <span class="help-inline">{{view.inputZodiac.validationResult.error}}</span>
        </div>
    </div>
    <div {{bind-attr class=":control-group view.inputPassword.validationResult.hasError:error:"}}>
        <label class="control-label" for="inputPassword">Password</label>
        <div class="controls">
            {{validator-text-field viewName="inputPassword" id="inputPassword" value=model.password}}
            <span class="help-inline">{{view.inputPassword.validationResult.error}}</span>
        </div>
    </div>
    <div {{bind-attr class=":control-group view.inputPassword.validationResult.hasError:error:"}}>
        <label class="control-label" for="inputPassword2">Password 2</label>
        <div class="controls">
            {{repeat-password-text-field id="inputPassword2" value=model.password2}}
        </div>
    </div>
    <div class="control-group">
        <div class="controls">
            <button type="submit" class="btn" {{action "clear"}}>Clear</button>
            <button type="submit" class="btn" {{action "validate"}}>Validate</button>
        </div>
    </div>
</form>
